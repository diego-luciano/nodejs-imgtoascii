<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Image to ASCII Converter</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous" />
    <link rel="stylesheet" href="css/style.css" />
</head>

<body>
    <div class="wrapper">
        <div class="container-fluid row p-5">
            <div class="form-col col-lg-12 mb-5">
                <h3 class="text-center font-weight-bold text-capitalize">
                    Image Parameters
                </h3>
                <div class="mt-3">
                    <form action="/" method="post" enctype="multipart/form-data">
                        <div class="form-group row">
                            <label class="col-4 col-form-label" for="width">Width</label>
                            <div class="col-8">
                                <input id="width" name="width" placeholder="50" type="number"
                                    aria-describedby="widthHelpBlock" required="required" class="form-control" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label for="height" class="col-4 col-form-label">Height</label>
                            <div class="col-8">
                                <input id="height" name="height" placeholder="50" type="number"
                                    aria-describedby="heightHelpBlock" required="required" class="form-control" />
                            </div>
                        </div>
                        <div class="my-4 row text-center">
                            <div class="col-12">
                                <span id="heightHelpBlock" class="alert alert-info">It works better with values
                                    around
                                    <span class="font-weight-bold">50</span> to
                                    <span class="font-weight-bold">100</span>.</span>
                            </div>
                        </div>
                        <hr>
                        <div class="form-group row">
                            <label for="height" class="col-4 col-form-label">Image</label>
                            <div class="col-8">
                                <input type="file" name="image" accept=".jpg, .png" required/>
                                <span id="heightHelpBlock" class="form-text text-muted">Formats: .jpg, .png</span>
                            </div>
                        </div>
                        <div class="form-group row">
                            <div class="col-12 text-right">
                                <button name="submit" type="submit" class="btn btn-primary">
                                    Upload
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
            <div class="ascii-col col-lg-12 text-center">
                <% if (!ascii) { %>
                <h3 class="display-4 font-weight-bold font-italic">
                    Submit your image
                </h3>
                <% } %> <% if (ascii) { %>
                <h3 class="display-4 font-weight-bold font-italic">Your ASCII</h3>
                <pre><%- ascii %></pre>
                <% } %> <% if (err) { %>
                <span class="alert alert-danger"><%- err %></span>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>